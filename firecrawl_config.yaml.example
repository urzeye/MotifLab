# Firecrawl 抓取配置（可选）
# 复制为 firecrawl_config.yaml 后修改

# 是否启用 Firecrawl 网页抓取
enabled: false

# API Key（云服务通常需要；本地部署可按实际情况留空）
api_key: ""

# 基础地址（留空默认走 https://api.firecrawl.dev）
# 本地部署示例: http://localhost:3002
base_url: ""
