# 图片生成服务配置（Docker 镜像内默认文件）
# 说明：
# 1) 容器启动后可通过 Web 设置页修改并持久化（建议挂载配置文件）。
# 2) API Key 支持直接明文，或使用 ${ENV_VAR} 从 .env / 环境变量读取。
# 3) active_provider 必须是 providers 中存在的名称。

active_provider: default

providers:
  default:
    type: image_api
    api_key: ${OPENAI_API_KEY}
    base_url: https://api.openai.com
    model: gpt-image-1
    endpoint_type: /v1/images/generations
    default_aspect_ratio: "3:4"
    image_size: 1024x1024
    use_size_tag: false
    high_concurrency: false
    short_prompt: false

  # gemini:
  #   type: google_genai
  #   api_key: ${GEMINI_IMAGE_API_KEY}
  #   model: imagen-3.0-generate-002
  #   high_concurrency: false

  # dashscope:
  #   type: dashscope
  #   api_key: ${DASHSCOPE_API_KEY}
  #   base_url: https://dashscope.aliyuncs.com
  #   model: qwen-image-max
  #   size: 1104*1472
  #   use_style_transfer: false
  #   edit_model: wan2.5-i2i-preview
  #   negative_prompt: ""
  #   prompt_extend: true
  #   watermark: false
  #   n: 1
  #   max_retries: 2
